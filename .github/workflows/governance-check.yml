name: Governance check

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify required folders
        run: |
          test -d constitutions
          test -d appendices
          test -d registers
          test -d tests-and-calibration
